<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Rikkahub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Material 3 theme with no action bar -->
    </style>

    <!-- Translucent theme for text selection overlay -->
    <style name="Theme.Rikkahub.Translucent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- Tool host: transparent background for embedded tool views -->
    <style name="Theme.Rikkahub.ToolHost" parent="Theme.Rikkahub">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>